# Core ML — pre-trained models, NO training required
torch>=2.0.0
transformers>=4.40.0
sentence-transformers>=2.7.0

# Data
pandas>=2.0.0

# Ollama Python client (optional — only needed if you want LLM grading)
# Install Ollama from https://ollama.com, then run: ollama pull llama3.2
ollama>=0.2.0
